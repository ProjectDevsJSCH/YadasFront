name: BEANSTALK_DEPLOY_DEVELOP

on: 
  push:
      branches:
      - develop

jobs:
  build:
    runs-on: ubuntu-latest
    environment: 
      name: develop
    strategy:
      matrix:
        node-version: [14.x]
        
    steps:
    - name: Checkout source code
      run: echo ${{ github.ref }}

name: BEANSTALK_DEPLOY_DEVELOP

on: 
  push:
      branches:
      - '**'

jobs:
  build:
    runs-on: ubuntu-latest
    environment: 
      if: ${{ github.ref == 'refs/heads/develop' }}
      name: develop
      if: ${{ github.ref != 'refs/heads/develop' }}
      name: review
    strategy:
      matrix:
        node-version: [14.x]
        
    steps:
    - name: Checkout source code
      run: echo "success"